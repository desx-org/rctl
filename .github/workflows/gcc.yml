# action.yml
name: 'Hello World'
on:
  workflow_dispatch:
    branches: [ master ]

description: 'Greet someone and record the time'
outputs:
  time: # id of output
    description: 'The time we greeted you'
runs:
  using: 'docker'
  image: 'Dockerfile'